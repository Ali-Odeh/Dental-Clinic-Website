-- Ali Odeh  


DDL Statements:

-- Users Table
CREATE TABLE users (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    role ENUM('Admin', 'Patient') NOT NULL
);



DML Statements:

-- Insert records
INSERT INTO users (name, email, password, role) VALUES 
('Ahmed Saleh', 'ahmed@gmail.com', '123', 'Admin'),
('Fatima Mohammed', 'fatima@gmail.com', '123', 'Patient');

-- Update record
UPDATE users 
SET name = 'Ahmed Ali', email = 'ahmed.ali@gmail.com'
WHERE user_id = 1;

-- Delete record
DELETE FROM users WHERE user_id = 2;
_________________________________________

DDL Statements:

-- Services Table
CREATE TABLE services (
    service_id INT AUTO_INCREMENT PRIMARY KEY,
    service_name VARCHAR(255) NOT NULL,
    description TEXT
);


DML Statements:

-- Insert records
INSERT INTO services (service_name, description) VALUES 
('Teeth Cleaning', 'Professional teeth cleaning service for a clean and healthy smile'),
('Teeth Whitening', 'Advanced teeth whitening solutions for a brighter smile');

-- Update record
UPDATE services 
SET service_name = 'Orthodontics', description = 'Teeth alignment service for improved appearance and function'
WHERE service_id = 1;

-- Delete record
DELETE FROM services WHERE service_id = 2;
_________________________________________

DDL Statements:

-- Messages Table
CREATE TABLE messages (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL,
    message TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    phone VARCHAR(15) NOT NULL
);



DML Statements:

-- Insert records
INSERT INTO messages (name, email, message, phone) VALUES 
('Yousef Adel', 'yousef@gmail.com', 'I need an appointment as soon as possible', '0598765432'),
('Sara Mahmoud', 'sara@gmail.com', 'What is the cost of teeth cleaning?', '0591234567');

-- Update record
UPDATE messages 
SET message = 'Can I change my appointment to another day?'
WHERE id = 1;

-- Delete record
DELETE FROM messages WHERE id = 2;

_________________________________________

DDL Statements:

-- Gallery Table
 CREATE TABLE gallery (
    id INT AUTO_INCREMENT PRIMARY KEY,
    image_path VARCHAR(255) NOT NULL,
    uploaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);



DML Statements:

-- Insert records
INSERT INTO gallery (image_path) VALUES 
('uploads/gallery/smile_1.jpg'),
('uploads/gallery/smile_2.jpg');

-- Update record
UPDATE gallery 
SET image_path = 'uploads/gallery/smile_updated.jpg'
WHERE id = 1;

-- Delete record
DELETE FROM gallery WHERE id = 2;

_________________________________________

DDL Statements:

-- Appointments Table
CREATE TABLE appointments (
    appointment_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    service_id INT NOT NULL,
    date DATE NOT NULL,
    time TIME NOT NULL,
    status ENUM('Pending', 'Approved', 'Rejected') DEFAULT 'Pending',
    rejection_reason TEXT NULL,
    FOREIGN KEY (user_id) REFERENCES users(user_id),
    FOREIGN KEY (service_id) REFERENCES services(service_id)
);



DML Statements:

-- Insert records
INSERT INTO appointments (user_id, service_id, date, time, status) VALUES 
(1, 1, '2024-01-10', '10:00:00', 'Pending'),
(2, 2, '2024-01-12', '12:00:00', 'Approved');

-- Update record
UPDATE appointments 
SET status = 'Rejected', rejection_reason = 'The appointment is not available'
WHERE appointment_id = 1;

-- Delete record
DELETE FROM appointments WHERE appointment_id = 2;

_________________________________________

DDL Statements:

-- Feedback Table
CREATE TABLE feedback (
    feedback_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    appointment_id INT NOT NULL,
    feedback_text TEXT NOT NULL,
    date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id),
    FOREIGN KEY (appointment_id) REFERENCES appointments(appointment_id)
);



DML Statements:

-- Insert records
INSERT INTO feedback (user_id, appointment_id, feedback_text) VALUES 
(2, 1, 'The service was excellent'),
(1, 2, 'The clinic is very clean and the staff are friendly');

-- Update record
UPDATE feedback 
SET feedback_text = 'The service was great, but I need more clarifications'
WHERE feedback_id = 1;

-- Delete record
DELETE FROM feedback WHERE feedback_id = 2;


 


 
